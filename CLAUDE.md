# CLAUDE.md — портфельный дашборд (Nordic/SEK)

Личный инвестиционный дашборд: vanilla-JS сайт + Cloudflare Worker + Supabase. Язык общения и UI-текстов — русский (UI двуязычный через `RT(ru,en)` / `T(key)`).

## Архитектура
- **Без сборки.** `index.html` грузит по порядку: `data.js` → `signals.js` → `chart.js` → `app.js` → `app-2.js` → `app-3.js` → `app-4.js` → `app-5.js` → `desk-selection.js` → `desk-journal.js` → `desk.js` → `desk-gloss.js` (классические `<script>`, общая глобальная область; функции + inline `onclick`). Затем — инлайновая регистрация SW. **Интерфейс — только Trade Desk** (с S7b-3): в `index.html` — skip-link и оверлеи (вход, справка, доступ, промпты, онбординг), `#desk` монтирует `desk.js`; `html.desk` ставит инлайн-скрипт `<head>` всегда, `<body class="v3">` держит токены `--v3-*` встроенных блоков (до S7b-4). Стили: `styles.css` (блоки классики, оверлеи) + `desk.css`.
- `data.js` — вынесенный блоб `ALL` (сид данных, старая схема индексов — в текущую её переводит `migrateState`). `app*.js` разбиты по доменам (одна глобальная область, границы по top-level `}`):
  - `app.js` — ядро: синк Supabase, вход/RBAC, i18n, FX, миграции (`migrateState`), перерисовка (`init`/`renderAll` → `deskRender`), свои вкладки, справка, график (`stockChartDraw`), фундаментал и betyg, AI Proto и плейбук, AI-разбор бумаги, «Состояние» (риск, cash-drag, FX-хедж);
  - `app-2.js` — «Состояние» (`pf3HealthTab`), дивиденды и отчёты, эффективный таргет (`pf3EffTarget`), AI-прогноз, диверсификация, «Добавить акцию», развитие портфеля и сравнения (`pfPerf*`), `renderPF3` → `deskRender`;
  - `app-3.js` — AI-портфель, сбор данных вселенной (`homeUpdateAll`), оценка (Valuation), сценарии bull/base/bear (`scenario*`, блок «Сценарии»), новости;
  - `app-4.js` — тезис-монитор (`cycle*`);
  - `app-5.js` — сделки, налоги K4, план, слой данных S3 (`POS_META`, план v2, `secFromRow`/`deskUniverse`), сигналы v2 (`sigSnapRow`, адаптеры для AI), цены/таргеты вкладок (`pf3FetchPrices`, `pf3RefreshTab`), пре/пост-рынок.
- **Supabase**: одна строка `ledger_state` с JSON-блобом (`data`), `rev` = optimistic concurrency (инкрементить при записи). На клиенте только anon-ключ. Реалтайм-синк: входящий снапшот дедуплицируется по `rev` (`syncRemoteDecision`, не по часам), при занятом синке откладывается в `remotePending` и применяется после push; push сериализован (`pushBusy`/`pushAgain`, `plans/sync-push-timer.md`); конфликт — облако побеждает, слияние по полям — блок E `plans/audit-followup.md`.
- **Cloudflare Worker** `telegram-notify.js`: live-цены/уровни (Yahoo), фундаментал (FMP→Yahoo), все AI-эндпоинты (Claude + web_search), Telegram-алерты, cron (AI-портфель + авто-анализ + bookcheck).

## Рабочий процесс (ВАЖНО)
- **Коммиты:** в сессиях Implementation редизайна (`plans/redesign-integration.md`, S1–S8) — один коммит в `main` в конце сессии после зелёных `bash tests/run.sh` (решение пользователя 2026-09-10). В остальных случаях коммитить только по явной просьбе. **Push — всегда только по явной просьбе.** Это solo-проект, коммиты идут в `main`.
- **Тесты:** `bash tests/run.sh` (osascript/JSC, конкатенирует `signals.js` + `chart.js` + `app*.js` + `desk-selection.js` + `desk-journal.js` + `desk.js` + `desk-gloss.js` + фикстуры + кейсы; есть и worker-suite). Pre-commit hook (`.githooks/pre-commit`) сам гоняет тесты и **авто-проставляет `?v=<хэш>`** ассетам в `index.html` — вручную версии не трогать.
- Чистые функции покрывать тестами в `tests/cases-app.js` (`__eq`/`__ok`/`grp`).
- Синтаксис без браузера: per-file `new Function(s)` через JSC (для воркера заменить `export default` перед проверкой).

## Процесс разработки фич (ВАЖНО)
Каждую новую фичу ведём по этому циклу — не перескакивать этапы:
1. **Setup** — окружение/контекст: какую часть `app*.js`/воркера трогаем, нужные данные, ограничения.
2. **Planning** — сформулировать задачу, согласовать подход, **зафиксировать план в файл `plans/<фича>.md`** (scope, файлы, решения, тест-кейсы). После этого **СТОП**: не писать код, дождаться новой сессии.
3. **Implementation** — реализуется в **НОВОЙ сессии**, которую запускает пользователь (для контроля прогресса). Я не начинаю код в той же сессии, где планировал. Новая сессия читает `plans/<фича>.md` как источник правды.
4. **Automated tests** — покрыть чистые функции в `tests/cases-app.js`, прогнать `bash tests/run.sh` + per-file JSC-синтаксис (Skill `check-syntax`).
5. **Code review** — самопроверка диффа (агентно/вручную), сверка с доменными правилами и архитектурой.
6. **Manual testing** — проверка в реальном окружении (сайт/воркер), без моков. Для воркера — Skill `verify-worker` после ручного деплоя.
7. **Documentation** — описание изменений (коммит/PR из диффа), обновление CLAUDE.md/памяти при необходимости.

**Ключевое правило (вариант 1, по решению пользователя):** Planning и Implementation идут в РАЗНЫХ сессиях. Я останавливаюсь после фиксации плана; новую сессию для кода открывает пользователь. Многофазные фичи: после этапа 6 возвращаемся к этапу 3 (тоже новая сессия) для следующей фазы. Коммит/пуш — только по явной просьбе (см. ниже).

## Worker — деплой ВРУЧНУЮ
- `git push` НЕ деплоит воркер. Пользователь деплоит `telegram-notify.js` сам.
- При правках воркера: бампать `WORKER_BUILD`, после деплоя проверять `?action=version`.
- AI-эндпоинты требуют admin Bearer-токен (Supabase-сессия). **Я не могу запускать AI-Proto/reco/dashboard сам** — это делает пользователь в браузере. Свой анализ даю через web_search.
- **Ошибки анализа в Telegram — с дедупом** (`errAlertEval`/`aiErrKind`, состояние `ai_state.alerts`, колонка из `ai-state.sql`): одна и та же ошибка по портфелю — не чаще раза в `ERR_ALERT_MS` (12 ч), после успеха — «✅ снова работает». Без колонки — шлёт каждый раз, как раньше.
- **Лондон (.L, `plans/lse-pence.md`):** Yahoo котирует LSE в пенсах (`GBp`) — воркер делит на 100 в точках входа `yChart` (`yChartNorm`) и `yQuoteSummary` (`yQsNorm`, белый список ценовых полей) до кэша; потребители видят фунты. Новый прямой fetch Yahoo в обход этих двух функций — нормализуй сам. Дивиденды Yahoo для LSE уже в фунтах.
- Беречь лимиты: FMP free ~250 запросов/день; не добавлять тяжёлые fetch'и в ежечасный cron.
- **Вызовы Claude — только через `anthropicRun`** (блок A аудита): стрим, повторы 429/5xx/529 и таймаут тишины (`AI_NET`), повтор усечённого json_schema-ответа (`AI_MAX_TOKENS_CAP`), затем ошибка `truncated`. Разбор структурного ответа — `aiParseJson`; нераспознанный ответ = ошибка, гейт (`lastRunAt`/`pfAnalysisAt`) не ставится. `ctx.waitUntil` в HTTP-роутах живёт ≤ 30 с после ответа — долгий AI-прогон в фоне не выживет. Сквозные тесты с моком fetch — `tests/run-worker-async.js` (node).

## Доменные правила
- Швеция: налог K4 — **genomsnittsmetoden** (средняя цена), НЕ FIFO.
- **Два betyg, не путать:** полный `pf3Betyg` (5 столпов из загруженного фундаментала; «Компания», модель отбора P1) vs `pf3RowBetyg` (lite по ROE/росту/оценке строки). AI-снапшоты грузят фундаментал всех позиций (`pf3LoadAllFundamentals`/`PF_FUND`) и используют ПОЛНЫЙ betyg; lite — фолбэк. Для финансов/PE (напр. EQT) ROE-lite искажает оценку.
- v3-вкладки: строки бумаг `DATA[tab].rows` (индексная схема колонок, `headers` не сокращать); встроенные блоки портфеля перерисовываются через `renderPF3` → `deskRender`; контекст блока — `deskCtx` (`curIdx`/`v3Key`/`pf3Tab`/`pf3Sel`).
- v2 (Svelte) удалён — v1 единственный продукт.
- **Позиция и план (S3 редизайна):** qty/средняя — в строке `r[6]/r[9]`; сторона/стоп/цель — в `POS_META[tab][TK]` (`stop0` = стоп входа, R считается от него). Мутаторы `posMetaSet`/`planMarkOpen` не сохраняют — вызывающий зовёт `scheduleSave()`. План v2 (`PLAN_RULES`): поля v1 не меняются, у «исполнено» источник правды `done`, `status` выводится `planRuleNorm`.
- **Сигналы v2 (S4 редизайна):** `signals.js` — чистый слой (глобал `SIG`, без DOM/глобалов приложения); **все пороги входа/выхода — только в `SIG.CFG`**. С S7b-3 — единственный движок решений: старые «Рекомендация»/фаза/«сигнал у уровня» и теневой журнал удалены (тест паритета фаз — на зафиксированных ожиданиях 20 строк). Пороги меняются только по решениям в `plans/signals-calibration.md` (калибровка 2026-09-10 — §5–6); при смене правил бампать `SIG.VER`. Поправки к фазе (Q7) — в `snapshot`, не в `phase`. **Адаптеры для AI** (app-5.js, s7b-map §5): фаза строки без свечей — `sigRowPhase` («—» без цены/SMA), снимок бумаги — `sigRowSnap` (сначала `deskItems`, т.е. тот же, что на экране, иначе `sigRowRead` — только чтение, риск `deskRiskKr` ради мемо), вердикт — `sigRowVerdict`, «у уровня» — `sigNearText`, карта для AI Proto — `sigRecoMap`; в снапшоты AI поле `recoVerdict`/`recoVerdicts` + легенда `SIG_AI_LEGEND` (промпты воркера описывают старую шкалу buy/wait/sell/avoid — соответствие в легенде). Бейдж «устарел» у AI-разборов — поле записи `sigAt` ≠ текущему вердикту (у старых записей `recoAt` — без бейджа). «Что-если» по порогам: `node plans/redesign-trading/whatif-signals.js [--ref=<git ref>] [--tg]`; вход против ориентиров (5 лет × вся вселенная, комиссия, бутстрэп — `signals-calibration.md` §8): `node plans/redesign-trading/replay-bench.js` — перегонять при смене `SIG.VER`. История вердикта — `SIG.replay` (вход = бар смены вердикта на buy/short, выход — лестница `simTrade`); `evalAt` должен оставаться причинным (тест «evalAt causal»).
- **График (S5):** `chart.js` — `chartModel` (чистая, тесты) + `renderStockChart` (цена — линия по закрытию, не свечи: решение 2026-09-11) (lightweight-charts 5.0.8 с jsdelivr + SRI в `loadLWC`); единственный вход из приложения — `stockChartDraw(state, boxId)` (графики «Акции»/инспектора desk). API v5: `chart.addSeries(LWC.LineSeries, …)`, маркеры — `LWC.createSeriesMarkers`; `addLineSeries`/`setMarkers` v4 больше нет. **Линия прибыли** (`plans/earnings-line.md`, только большой график desk «Акция»): чистая `chartEarningsModel` (EPS фин. года × «нормальный» P/E, пунктир — консенсус Yahoo, будущее — пустые слоты рабочих дней) + `chartEarnAt`; «нормальный» P/E — своя история на тех же данных (`chartEarnPe`: цена на конец FY ÷ EPS, 5 лет свечей `histBars(sym,'5y')`) → `deskEarnPe` (своя медиана, если устойчива и не дальше ×histX от TTM P/E → сектор → текущий по FY; FMP `VAL.hist` не используется), пороги — `DESK_IDEA_CFG.earn`, аудит по 379 бумагам — `plans/earnings-line.md`; вход — `state.earn` в `stockChartDraw` (мини-график — без него). Переключатель «Прибыль» — localStorage `dash_desk_earn`; `?financials=` и 5 лет свечей грузятся при входе в режим «Техника» «Акции», только если линия включена. Цвет — токен `--dk-earn`.
- **Trade Desk (S6; единственный UI с S7b-3):** `desk.js`/`desk.css`. Флага `dash_desk` больше нет — `?desk=0` и старое значение в localStorage игнорируются, `deskActive`/`deskClassic`/«Выключить Trade Desk» удалены. `renderAll`/`renderPF3` — обёртки над `deskRender()` (первый `init()` из `boot()` идёт до загрузки desk.js — рисует `deskBoot`). Экраны строят HTML из глобалов (`deskUniverse` + `sigSnapRow`, `bookPositions`, `PLAN_RULES`, `PF_TRADES`), события — делегированием по `data-a`/`data-c` на `#desk`. Сделка из desk — только через `deskExecApply` (позиция + кэш + `PF_TRADES` + `POS_META` + план). Чистое ядро (`deskScreenRows`, `deskTodayBuckets`, `deskPosAct`, `deskRoundTrips`, `pfApplyTradeSide`, `deskCapCheck`…) — в начале файла, покрыто тестами; в тестовом раннере `deskBoot()` вырезается. Шорт: `pfEquitySEK` считает его результатом, кэш при открытии меняется только на комиссию.
- **Перенос классики (S7b-2, `plans/s7b-map.md` §9; удаление — S7b-3, §10):** классических экранов нет; их нужные блоки встроены в desk. Разделы книги `DESK_UI.bt` (Позиции · Структура · Дивиденды и отчёты · Состояние · Статистика · AI; AI-портфель `AIP_KEY` — фильтр селектора только на маршруте `book`, `deskRiskTab()` его не отдаёт — риск/«Что если?»/исполнение/мемо SIG не трогаются), журнал `DESK_UI.jt` (+ Записи · Налоги · AI-разборы, «✏️ Редактор плана» в «Планах»), экран `#desk/service` (⋯ → «🧰 Сервис»: вкладки и бумаги, сбор данных). **Встраивая блок классики:** контекст раздела — `deskCtx(tab, sub, sel)` до отрисовки (хвосты сверяют `isV3()`/`pf3Tab` и зовут `renderPF3 → deskRender`), разовый блок в чужом портфеле — `deskWithCtx(tab, fn)`; загрузку раздела зовите **один раз на вход** (`deskClassicAfter`), не на каждой перерисовке (неудача без кэша зациклит сеть); поля с id внутри `.dk-classic` переживают фоновые перерисовки (черновики `DESK_UI.draft`); поллеры пре/пост (`cardPPStart`, `pfSumPPStart`) и синк AI-портфеля включает/гасит `deskClassicAfter` по наличию контейнера. Открыть «Акцию» по тикеру из блока классики — `deskOpenTk(tk)` (`simOpen`/`insiderOpenCard` в desk уже зовут его). Правка позиции без сделки — `deskPosFix`. Свежие цены/таргеты одной вкладки из встроенного блока (AI-прогон, «Добавить акцию», 🔄 «Статистики») — `pf3RefreshTab(key)`.
- **Список покупок (I1, `plans/reference-features-implementation.md`):** ключ снапшота `deskWatch` = `DESK_WATCH` (app.js) с защитой от старого клиента и бэкапом; нормализация/мутаторы `deskWatch*` — в app-5.js (не сохраняют — зови `scheduleSave()`); все пороги новых функций (зона, справедливая стоимость, уровень риска 1–5) — только в `DESK_IDEA_CFG`. Чистые `deskWatchZone`/`deskFairValue`/`deskRiskLevel`/`deskBuyDefault` — в ядре desk.js. «Как рассчитано» — `dkHow(id, html)` (закрыто, открытые помнит localStorage `dash_desk_det`). «Что если?» (I2): чистая `deskWhatIf` в ядре desk.js ничего не пишет (тест сравнивает состояние до/после) и считает только через `tradeFeeNative`/`FX`/`bookRiskState`+`deskCapCheck`/`bookPositions`/`pfApplyTradeSide`; настройки — `DESK.whatIf` (через `deskNorm`), пороги — `DESK_IDEA_CFG.whatIf`. «Рост бизнеса» (I3): воркер `?financials=SYM` (FMP US / Yahoo timeseries + прогноз Yahoo `earningsTrend`, чистые `finAnnualFmp`/`finAnnualYahoo`/`finEstimatesYahoo`/`finBuild`, пороги `FIN_CFG`); на клиенте запрос — **только по раскрытию** блока (`DESK_UI.finOpen`, не localStorage), модель `deskFinModel` + inline-SVG `deskFinSvg`. «Аналитики и оценка» — только клиентские данные (`VAL`, `TG_FULL`, колонка «Кап-я»); веер цели — `chartFanModel`/`renderTargetFan` в chart.js; пороги — `DESK_IDEA_CFG.fin`/`.ana`.
- **Словарь Trade Desk (G1, `plans/desk-glossary.md`):** `desk-gloss.js` — `DESK_GLOSS` (разделы + записи), единственный источник текстов словаря (⋯ → «📖 Словарь», клавиша `?`, кнопка в «❓ Справке») и подсказок на экранах (G2). **Новый термин/метка в UI desk → запись в `DESK_GLOSS`**; метки — id `v-*`/`ph-*`/`fl-*`/`act-*` по ключам `DK_V`/`DK_PH`/`DK_FLAG`/`DK_ACT` (тест покрытия упадёт). **Пороги в текстах — только из `SIG.CFG`/`DESK_IDEA_CFG`/`deskNorm`** (поле записи — функция `()=>string`), числа сквозного примера считает `deskGlossStory()` через `SIG.tradePlan`/`limitForRR`/`posCalc`/`deskPosAct`/`deskTrailStop`. Тексты описаний — только RU, кнопки/подписи UI — `RT`. Панель — drawer `#dkGloss` внутри `#desk` (не перерисовывается `deskPaint`), состояние `DESK_UI.gloss` (память, не снапшот), `deskGlossOpen(id)` открывает на записи `dkg-<id>`; пока открыта — `#dkRail/#dkMain/#dkModal` inert, клавиши экрана не работают, Esc закрывает. **Подсказки (G2):** метка в UI desk с термином → `dkG('<id>')` (наведение/долгое нажатие), заголовок KPI/раздела → `dkGi('<id>')` (ⓘ); id — только литералом или из ключей `DK_*` (тест сканирует desk.js и падает на id без записи). Всплывашка `#dkTip` (desk-gloss.js, `deskTipShow/Hide/AfterPaint`, пороги `DK_TIP`) живёт вне `#dkMain`; текст — `deskGlossTip(id)` = первое предложение `d` (короче 50 знаков — со вторым), поэтому **первое предложение описания должно объяснять термин само по себе**. Не ставить `title` на элемент с `data-g` — будет две всплывашки.
- **Модель отбора компаний (P1, `plans/stock-selection-ux.md`):** `desk-selection.js` — чистый модуль (`deskSelectionModel`, `deskPickBuckets`; без глобалов приложения, всё аргументами — тест грузит его изолированно); адаптер `deskSelectionInput`/`deskSelectionModels` и конфигурация `deskSelectionConfig` — в desk.js, UI-лимиты — `DESK_IDEA_CFG.selection`. Четыре измерения (качество = бизнес-столпы `pf3Betyg` без `val`, веса — общий `PF3_BETYG_WEIGHTS`; цена; момент SIG; риск), **нового общего score нет** — порядок внутри подборки `SIG.cmp`, затем ключ. Оценка для «Кандидатов» не обязательна; «Нужно изучить» — только технически проходящие бумаги, которым не хватает данных. Адаптер читает `sigSnapRow(…, readOnly=true)` (не пишет мемо/тень) с риском `deskSelectionRiskKr` = как у `deskItems`, иначе мемо `SIGNALS` промахивается. Старые кэши по тикеру (`VAL`, `TG_FULL`) годны только с `ccy`/`sym` листинга (`deskSelectionCache`), иначе `cache-incomparable`.
- **«Акция» в трёх режимах (P2, `plans/stock-selection-ux.md` §4/§17):** `DESK_UI.stockView` = `decision|company|tech` (память); адрес `#desk/stock/<ключ>[/company|/tech]` — `deskHashOf`/`deskHashParse`, «Решение» в адрес не пишется. Прямое открытие бумаги — `deskGo('stock',key,'decision')`; смена режима — только `deskStockView(v)` (replaceState); прокрутка записи истории — `history.state.dkY` (`deskHistSave`, `DESK_UI._restoreY`). «Решение» строится из модели P1 (`deskStockModel` → `deskSelectionModel`) и **не шлёт запросов**; primary-кнопка одна — `deskDecisionBtn` (чистая, id → `dkDecBtn`), новые выводы → запись `dec-*` в `DESK_GLOSS` (тест покрытия `DK_DEC`). Сторона вне «Техники» — `deskStockSide` (позиция или лонг), `DESK_UI.side` действует только в «Технике»/инспекторе. Новые загрузки бумаги — только через пул `deskPoolRun('<тип>|<символ>', fn)` (лимит `DESK_IDEA_CFG.selection.loadPool`, дедуп, фоновая перерисовка); «Компания» грузит при входе (`deskCompanyEnsure`, раз на вход). Большой график — только в «Технике»; бросать состояние графика — только через `deskChartKill` (destroy + сдвиг `_tok`, иначе поздний `stockChartDraw` дорисует второй канвас). Встроенные блоки классики (AI-разбор, AI-анализ, тезис-монитор) читают `v3Key/pf3Sel` — любые данные, собираемые после `await`, брать по строке запуска, не по текущему выбору (пример — `stockAiSnapshot`). Карта переноса классической карточки для S7b — §17.3 плана.
- **Журнал результатов выбора (P5a/P5b, `plans/stock-selection-ux.md` §7/§20/§21):** `desk-journal.js` — отдельный локальный слой, не часть снапшота/ledger/`shared_analysis`/воркера; подключён `desk-selection.js → desk-journal.js → desk.js`. Чистые `deskJournalRecord`/`deskJournalAdd`/`deskJournalPatch`/`deskJournalCleanup`/`deskJournalClearCompleted`/`deskJournalQuotaState`/`deskJournalExportJSON` — без localStorage/DOM/`currentUser`, время и id только аргументами (`ctx.now/ctx.id`); после записи менять можно только `entry`/`horizons`/`status` (`deskJournalPatch` игнорирует остальные поля), удаления по id нет. Дедуп — бумага+`selectionVersion`+UTC-дата `recordedAt` (`deskJournalDedupKey`). Лимит — 500 записей ИЛИ 500 КБ JSON (`DESK_JOURNAL_CFG`), что раньше; при достижении `deskJournalAdd` отказывает без изменения списка, `pending` по возрасту никогда не удаляется (`deskJournalCleanup` трогает только `complete` старше 365 дней после последнего горизонта; `deskJournalClearCompleted` — ручной эскейп-клапан «очистить завершённые сейчас», тоже мимо `pending`). Хранение — `dash_desk_journal_<currentUser.id>` (пространство аккаунта, как `dash_bak_<id>`); без аккаунта `deskJournalRecordAdd` явно отказывает (`no-account`), не пишет анонимно; ошибки `localStorage.setItem` возвращаются вызывающему, не проглатываются. Нечитаемый/новой версии журнал — `deskJournalRead` даёт причину, запись/очистка/патч отказываются (не затирать). **P5b (§21):** расчёт — чистый `deskJournalEval` (серии/время/комиссия аргументами) в том же файле: условный вход = закрытие первой сессии биржи, начавшейся после записи (местная дата/часы — `DESK_JOURNAL_SESSIONS` по суффиксу символа; бар окончательный — загрузка после close+`settleMin` или есть следующий бар); горизонты 20/60/120 баров, вход и завершённые горизонты не пересчитываются; индекс ^GSPC/^OMX по валюте (`deskJournalBenchmark`), альфа только на общих датах; сплит/масштаб — `adjTol`/`jumpX` → «нет результата», не ноль; net — `tradeFeeNative` на сумму `DESK.whatIf` записи, версия `deskJournalCostV`; группы `deskJournalGroups` (версия отбора · SIG · сторона · горизонт, порог `minGroup`). Все пороги — `DESK_JOURNAL_CFG`. Версия правил отбора — `DESK_SELECTION_V` (desk-selection.js): **бампать при смене правил `deskSelectionModel`/подборок**. UI — desk.js: кнопка `deskJournalTrackBtn` в «Решении» (вторичная, без открытой позиции), вкладка «Журнал → Наблюдения» (`DESK_UI.jt='ideas'`), догрузка — `deskJournalEnsure` только на этой вкладке (пул `jhist|`, общий `_histCache`), запись результата — `deskJournalPatchAndSave` поверх свежего списка.
- **Сравнение 2–4 бумаг (P4, `plans/stock-selection-ux.md` §6/§19):** выбор — `DESK_UI.compare.keys` (биржевые ключи, память вкладки, до `DESK_IDEA_CFG.selection.maxCompare`; мутировать только через `deskCmpToggleKey`/чистую `deskCompareToggle`), читать — через `deskCompareKeys()` (сверяет аккаунт `currentUser.id` и доступ, убранное считает в `dropped`). Чекбокс — `dkCmpBox(key)`: label `data-a="noop"`, событие у `input` (иначе клик откроет карточку); в список он ничего не пишет. Маршрут `#desk/compare` без ключей. Строки и сопоставимость — чистая `deskCompareModel` в desk-selection.js (`DESK_CMP_ROWS`): подсветка максимума/минимума только если значение есть у всех, применимо, не предварительное и одного базиса (период/сторона/источник/набор столпов), с точностью показа 0,1; нового общего балла и «победителя» нет. Рост/FCF — последний FY из `?financials=` (`deskCompareFinFacts`), не `PF_FUND` (у Yahoo FCF там TTM). Один портфель на все колонки (`deskCmpPort`); «Что если?» — `deskCompareWhatIf` по каждой бумаге отдельно против текущего кэша (не корзина). Загрузки — `deskCompareEnsure` при входе через пул. Новая строка сравнения → `DK_CMP_ROW` с записью словаря (тест покрытия).
- **bookcheck (S8, воркер):** cron-слоты `pickCronTask` (:00 AI-портфель, :20/:40 анализы, остальные тики — bookcheck); чистые `bookItems` (снапшот → условия стоп/цель позиции из `POS_META`/open-правила и лимит/«сетап сломан»/стоп правил плана — та же семантика, что `planStatus`), `bookEval` (дедуп с гистерезисом `BOOK_CFG.hystAtr`·ATR, ключ содержит уровень), `bookLine`/`bookMessage`. Состояние дедупа — только `ai_state.book` (не в ledger: клиент затёр бы); порядок «сохранить состояние → Telegram», без колонки не шлёт. Выключатель — `DESK.tg` (`snap.desk.tg === false`). Меняешь семантику `planStatus`/`POS_META` на клиенте — поправь `bookItems`. Тикеры в плане сравнивать через `planTkKey`/`bkKey` («INVE B» = «INVE-B»); уровень из совета AI — `planParseLevel` (только цена входа, таргет не берётся). Запись воркера в ledger — через `writeChecked` (RMW с повтором по rev), «успех»/Telegram только после коммита.
- **Шорт в журнале:** `PF_TRADES` с `short:true` (открытие `sell`, закрытие `buy`); `pfTaxLots` признаёт результат в дату откупа по средней выручке.
- **Новый ключ снапшота** = `snapshotState` + ветка в `applyRemoteState` + `SNAP_KEYS` в `tests/cases-app.js` (round-trip тест упадёт, если забыть). Ключ **удаляем** только если старый клиент без него не теряет данные (`layout`/`aiPrefs` убраны в S7a; `rankings`/`sma`/`colOrders`/`hiddenCols`/`tabGroups`/`tabOrder` — в S7b-3). Замороженные до блока E (`plans/audit-followup.md`) ключи удалённых функций — `sim`, `aiDash`, `scnAlerts`, `tgAlerts`, поля вкладок `btJournal`/`btConfig`, `SMA_TF.w`: данные не трогаем, писателей нет.
- **Миграции (S7a):** `migrateState()` — один проход на загрузку состояния (`boot()` для бандла и `applyRemoteState()` для облака), **не** в `init()`. Все шаги идемпотентны; одноразовый шаг = блок `if(STATE_V<N)` в `migrateSchema` + `SCHEMA_V=N` (сейчас 3: v3 — `migrateSmaDaily`, SMA в колонках только дневные). Поля, которые может снова записать клиент на другом устройстве, чистятся **не одноразовым шагом, а на каждый проход** `migrateState` (идемпотентно, с `scheduleSave`): `migrateDropReco` — колонка «Реком. скоринг» и `btSignals`/`btRuleAcc` вкладок (S7b-3); сиды не должны проверять «вкладки нет → создать» без гейта `STATE_V`. Личных сидов (позиции, кэш, семейные портфели) в коде нет. Флаги удалённых старых шагов в данных (`brokerSnap`, `cashSnap`, `gsSeed`, `scSeed`, `aiMig`, `ttlMig`) **не удалять**, пока может быть открыт клиент до S7a — без флага он применит шаг заново.
- **Оболочка (S7b-3):** классической шапки, вкладок, групп навигации, таблиц, Home и карточки нет; `html.ui2` не ставится. Свои вкладки и бумаги — «⋯ → 🧰 Сервис». В колонку «Реком. скоринг» клиент не пишет (воркер добивает пустую `aipVerdict`). Шрифты: Golos Text (кириллица) + JetBrains Mono.

## Память
Персистентная авто-память в `…/memory/` (`MEMORY.md` = индекс). Не дублировать сюда то, что уже там; обновлять заметки при изменениях.
