# ТЗ — Переработка страницы 🏠 HOME

**Статус:** черновик на согласование · **Дата:** 2026-06-17
**Контекст:** HOME уже существует (`HOME_KEY='🏠 Home'`, `homeHTML()` в app.js). Это **доработка**, а не новая страница. Реюз существующих функций обязателен.

---

## 1. Цель

Главная страница-«пульт»: за один экран видно состояние рынков и куда смотреть. Три задачи из запроса:

1. **Все индексы с уровнями поддержки/сопротивления** (сейчас у индексов только цена + дневное %).
2. **Единый список лучших акций по ВСЕМ сигналам сразу** (сейчас — 3 раздельные таблицы по горизонтам, нет сводного рейтинга).
3. **Удобный дизайн и расположение кнопок** (сейчас кнопки разбросаны по панелям).

---

## 2. Что уже есть (реюз, не переписывать)

| Блок | Функция | Примечание |
|---|---|---|
| Live-плитки фьючерсов/индексов | `homeFutTiles`, `homeMktInner`, `homeLoadFutures` (обновление 20с) | `HOME_MKT_FUT` (7), `HOME_MKT_IDX` (6) — app.js:5438 |
| Лучшие по 3 горизонтам | `homeBestPicks`, `homeBestHTML`, `bpWhy*` | детерминированный скоринг sShort/sMed/sLong |
| Прогноз топ-10 | `homeForecastPicks`, `homeForecastHTML`, `homeFcastAiRun` | путь к таргету + AI-версия |
| Кнопки-инструменты | панель `tools` в `homeHTML` | «🔄 Обновить всё», «🕵 AI Insider», «📐 Оценка» |
| Скоринг акций | `pf3Reco`, `pf3RecoHorizons`, `pf3Criterion`, `pf3EffUpside`, `pf3TypeMetrics`, `signalScore` | по строке `r[]` |
| Сигналы (глобалы) | `VAL[tk]`, `INSIDER[tk]`, `AI_RECO[tk]`, `TG_FULL[tk]` | app.js:442–453 |
| Уровни по акциям | колонки `Поддержка`/`Сопротивление` + `smaIdx` (SMA 50/100/200) | у **индексов отсутствуют** |
| Тех-данные индексов | `SMA_IDX` (price, sma50/100/200, signal) | через `renderSMA` |

---

## 3. Блок A — Индексы с поддержкой/сопротивлением

### 3.1 Что показать
Карточка на каждый индекс/фьючерс (объединить текущие `HOME_MKT_FUT` + `HOME_MKT_IDX`):

```
┌─ S&P 500 (ES=F)            ● LIVE ─┐
│  5 412.30      ▲ +0.42%            │
│  ───────────────────────────────  │
│  R2  5 498   ·  R1  5 455          │   ← сопротивления (выше цены, красным)
│  ▸ цена 5 412                      │   ← маркер текущей цены в «лестнице»
│  S1  5 370   ·  S2  5 328          │   ← поддержки (ниже цены, зелёным)
│  тренд: ▲ выше SMA50/200           │   ← из SMA_IDX
└────────────────────────────────────┘
```

- Уровни **выше** цены → сопротивление (R), **ниже** → поддержка (S), отсортированы по близости.
- У каждого уровня — % расстояния от цены (как `lvl()` в `pf3DetailHTML`, app.js:5655).
- Подсветка ближайшего уровня (≤1% — «у уровня»).
- Тренд-строка: цена vs SMA50/SMA200 (есть в `SMA_IDX`).

### 3.2 Источник уровней — НОВЫЙ воркер-эндпоинт `?levels=`
У индексов нет ни колонок S/R, ни опционов. Считаем на воркере из дневной истории (есть `dailyHistory(sym)`):

`GET ?levels=ES=F,NQ=F,^OMX,…` → `{ "ES=F": {price, pct, sma50, sma200, sup:[S1,S2], res:[R1,R2], pivot}, … }`

**Метод (рекомендуемый — гибрид, детерминированный):**
1. **Daily pivot points** от вчерашнего бара (H,L,C):
   `P=(H+L+C)/3`, `R1=2P−L`, `S1=2P−H`, `R2=P+(H−L)`, `S2=P−(H−L)`.
2. **Swing-уровни** как страховка/дополнение: `R = max(High, 20 дней)`, `S = min(Low, 20 дней)`; плюс 52-нед макс/мин для дальних уровней.
3. Итог: до 2 ближайших сопротивлений выше цены и 2 поддержек ниже (объединить пивоты + свинги, схлопнуть уровни в пределах 0.3% друг от друга).

**Чистая функция (тестируемая):** `indexLevels(closes, highs, lows)` → `{pivot, sup:[…], res:[…]}`. Покрыть `cases-worker.js` (пивоты на известном баре, сортировка/схлопывание).

**Бюджет subrequest:** один `?levels=` тянет ~13 символов; каждый — 1 history-fetch. Чтобы не упереться в лимит воркера — **чанками по 5–6 символов** (как сделали для targets/valuation) ИЛИ кэш на воркере на 10–15 мин (S/R меняются медленно). Рекомендация: серверный кэш по символу + отдача всех разом.

**Клиент:** `homeLevels={}` + `homeLoadLevels()` (как `homeLoadFutures`), интервал — реже фьючерсов (S/R статичны): раз в 5 мин. Live-цена остаётся из `?symbols=` (20с), уровни — фон.

---

## 4. Блок B — Единый рейтинг лучших акций «по всем значениям»

### 4.1 Идея
Один сводный борд `🏆 Лучшие акции — общий рейтинг` поверх (или вместо) трёх горизонтных таблиц: **композитный балл из всех доступных сигналов**, top-N, с разбивкой почему.

### 4.2 Композитный балл — `homeBestComposite()` (новая чистая функция)
Собрать по всем тикерам из `v3Tabs()` (дедуп по тикеру, как `homeBestPicks`). Для каждого посчитать нормированные суб-сигналы (0..1 или −1..1), затем взвешенная сумма:

| Сигнал | Источник | Вес (предложение) |
|---|---|---|
| Апсайд к таргету | `pf3EffUpside(d,r)` | 0.20 |
| Недооценка (P/E vs медиана сектора) | `signalScore`/`VAL[tk]` | 0.15 |
| Качество (ROE) | `pf3TypeMetrics.roe` | 0.10 |
| Рост выручки | `pf3TypeMetrics.revg` | 0.10 |
| Тех-фаза | `pf3Criterion(d,r).rank` (0 нож … 8 недооценка) | 0.15 |
| Близость к точке входа | мин. \|dist\| до SMA50/Support | 0.10 |
| Рекомендация движка | `pf3Reco(d,r).v` (buy/wait/sell/avoid) | 0.10 |
| Инсайдеры | `INSIDER[tk]` (нетто-покупки/кластер) | 0.05 |
| AI-вердикт | `AI_RECO[tk].verdict` | 0.05 |

- Нормировка каждого сигнала своей шкалой (clamp), штрафы за «падающий нож» / `avoid` / перегрев.
- Отсутствующие данные → вклад 0 (не штраф), чтобы EU/Nordic без VAL/AI не проваливались.
- Итог: `score` 0..100 + массив `signals[]` сработавших (для чипов).
- **Сортировка/фильтры:** сегмент по умолчанию «общий», плюс быстрые сорты: апсайд / недооценка / качество / у входа. Реюз горизонтных скорингов для под-вкладок.

### 4.3 Вид
Таблица `bp-tbl` (уже есть стили), строки кликабельны → `insiderOpenCard(tk)`:

```
#  Акция            Цена      Балл   Сигналы
1  Micron  MU       142.10    87 ███  +28% к таргету · ROE 21% · у SMA50 · инсайдеры↑
2  …
```

- Колонка «Балл» — число + мини-бар (как `sect-bar`).
- «Сигналы» — топ-3 чипа из `signals[]`.
- Чип-легенда и сорт-кнопки над таблицей (сегмент-контрол `pf3-hz-seg`, уже стилизован).

### 4.4 Совместимость
- Горизонтные таблицы (`homeBestHTML`) — оставить **под** сводным бордом, свернуть в `<details>` («Разбивка по горизонтам 1–3 / 3–6 / 6–12 мес»). Не выбрасываем готовую логику.

---

## 5. Блок C — Дизайн и расположение кнопок

### 5.1 Структура страницы (сверху вниз)
1. **Шапка HOME** — статус рынка (🟢/🌅/🌙/🔴, как `sectMktLbl`) + время апдейта + **первичная кнопка «🔄 Обновить всё»** (крупная, акцентная, прижата вправо).
2. **Полоса индексов** (Блок A) — адаптивная сетка карточек (`fut-grid`, расширить под S/R).
3. **🏆 Общий рейтинг акций** (Блок B) — headline-борд.
4. `<details>` Горизонты (свёрнут) + 🔮 Прогноз.
5. **Админ-зона** (только `isAdmin()`): signal / val / insider — под единым заголовком «🛠 Инструменты сбора данных», кнопки сгруппированы (🕵/📐/🔄 AI-Рекомендация) в один ряд.

### 5.2 Группировка кнопок (сейчас разбросаны)
- **Первичное действие:** одна заметная «🔄 Обновить всё» в шапке (не дублировать в каждой панели).
- **Сегмент-контролы** (горизонт/сорт) — стиль `pf3-hz-seg .pf3-hz-b` (уже есть).
- **Админ-инструменты** — собрать в один тулбар-ряд с тултипами, визуально отделить (рамка/фон), чтобы не путать с навигацией.
- **Доступность:** учитывать RBAC — `homeFcastAiRun`/insider/val уже под `isAdmin()`; ничего платного не показывать обычным юзерам.

### 5.3 Адаптив
- Сетка индексов: `repeat(auto-fill, minmax(220px, 1fr))`.
- На узком экране карточка индекса схлопывает дальние уровни (R2/S2), оставляя R1/цена/S1.
- Все новые стили — **в конец `styles.css`** (append-only).

---

## 6. Изменяемые/новые артефакты

**Воркер (`telegram-notify.js`) — нужен ручной редеплой:**
- `indexLevels(closes, highs, lows)` — чистая, пивоты+свинги.
- `?levels=` GET-роут (чанки/кэш).
- `WORKER_BUILD` → bump.

**Клиент (`app.js`):**
- `homeLevels`, `homeLoadLevels`, интеграция в карточку индекса (расширить `homeFutTiles`/`homeMktInner` или новый `homeIdxCard`).
- `homeBestComposite()` + рендер `homeBestBoardHTML()`; перестроить `homeHTML()` (порядок блоков, шапка со статусом, `<details>` для горизонтов).
- Сорт-сегменты борда.

**Стили (`styles.css`, append-only):** карточка индекса с лестницей уровней, мини-бар балла, чипы сигналов, админ-тулбар.

**Тесты:**
- `cases-worker.js`: `grp('indexLevels', …)` — пивоты на эталонном баре, сортировка, схлопывание.
- `cases-app.js`: `grp('homeBestComposite', …)` — нормировка, штраф за нож/avoid, отсутствие данных = 0, корректный топ.

**Версии:** bump `?v=` в index.html (3 ссылки); `WORKER_BUILD`.

---

## 7. Решения по умолчанию (можно переопределить)

1. **S/R индексов** — гибрид «daily pivots + 20-дн свинги» (а не только пивоты). *Альтернатива: только пивоты — проще, но грубее.*
2. **Источник уровней** — воркер (`?levels=`), не клиент: история и так на воркере, кэш дешевле.
3. **Общий рейтинг** — добавить как headline, горизонты свернуть (не удалять).
4. **Кэш уровней** — 10–15 мин на воркере; live-цена остаётся из `?symbols=`.

---

## 8. Этапы (предлагаемый порядок)

1. **Воркер:** `indexLevels` + `?levels=` + тест + редеплой. *(разблокирует Блок A)*
2. **Клиент Блок A:** карточки индексов с уровнями.
3. **Клиент Блок B:** `homeBestComposite` + борд + тест.
4. **Клиент Блок C:** перекомпоновка `homeHTML`, шапка, группировка кнопок, стили, адаптив.
5. Версии, прогон тестов, коммит/пуш.

---

## 9. Критерии приёмки
- [ ] Каждый индекс на HOME показывает ≥1 поддержку и ≥1 сопротивление с % дистанции; ближайший подсвечен.
- [ ] Есть единый рейтинг акций с композитным баллом и чипами сигналов; клик открывает карточку.
- [ ] Горизонтные таблицы и прогноз сохранены (свёрнуты).
- [ ] «Обновить всё» — одна первичная кнопка в шапке; админ-инструменты сгруппированы; обычный юзер не видит платных кнопок.
- [ ] `bash tests/run.sh` зелёный (новые группы indexLevels + homeBestComposite).
- [ ] Адаптив: на узком экране карточки и борд не ломаются.
