# План слияния: прототип «Trade Desk» + аудит + текущий portfolio-dashboard

Статус: **Planning** (2026-09-10). Дополняет `plans/redesign-trading.md` (что строим) ответом на вопрос **как** это встроить в живое приложение без остановки работы. Implementation — отдельными сессиями по §7.

## 1. Принцип: пересадка органов, не пересборка

Приложение продолжает работать всё время слияния. Новые модули подключаются **рядом** со старыми файлами, старые экраны заменяются по одному за флагом, данные и синк не меняют формат до последнего этапа. Три правила:

1. **Один источник правды на каждую сущность.** Сигналы — `signals.js`, график — `chart.js`, позиция — `POS_META`, план сделки — `PLAN_RULES`. Старые движки живут, пока новый не подтверждён теневым сравнением, затем удаляются.
2. **Флаг `desk`.** `localStorage.dash_desk = '1'` (и `?desk=1` в URL) включает новую оболочку; без флага — текущее приложение. Флаг снимается на этапе чистки, когда старые экраны удалены.
3. **Аудит закрывается по ходу, а не отдельным проектом.** Каждая сессия слияния закрывает список находок (§6); quick wins идут первыми, потому что они снижают риск самого слияния (синк, воркер, тесты).

## 2. Карта соответствий: прототип ↔ приложение

| В прототипе | В приложении сейчас | Как сливаем |
|---|---|---|
| `OHLCV[tk].bars` (свечи 2 года) | `?history=` → `{t,c}` только закрытия; `_histCache` (app.js:2138) | воркер `dailyHistory` (telegram-notify.js:451) отдаёт `{t,o,h,l,c,v}`; клиент — `histBars(sym)` с кэшем 10 мин, старый формат терпится (o=h=l=c) |
| `U[tk]` — бумага: tk, name, ccy, idx, sector, fund | строка `r[]` вкладки (`r[1]` имя, `r[2]` тикер, `r[4]` сектор, `r[7]` цена, `r[8]` валюта) + `VAL/TG_FULL/AI_RECO` по тикеру | адаптер `secFromRow(d, r)` → объект бумаги; дедуп по `exSymbol(r[2], r[8])`; вселенная = все вкладки, а не только `v3Tabs()` |
| `SIG.snapshot(bars, opts)` | `pf3SignalInfo`, `pf3Criterion`, `pf3Reco`, `pf3RecoHorizons`, `scenario*`, `homeBestPicks`, `homeCompositeScore`, `planStatus`, `aipVerdict` (воркер) | `signals.js` подключается до `app.js`; `SIGNALS[tk]` считается один раз на обновление данных; старые функции читают из него через прокси до удаления |
| `SIG.tradePlan(side, levels, atr, {riskKr, fx, entry, half})` | `scenarioShort` (R/R), `planShares` (сумма → штуки) | заменяет оба; `riskKr` — новый синк-ключ `desk.riskKr` |
| `SIG.phase()` | `pf3Criterion` (app-2.js:256) | порт 1:1, тест паритета на 20 строках перед удалением оригинала |
| `renderStockChart(el, bars, snap, {side, plan, layers, theme})` | `drawChart` (app.js:2161), `openStockChart` попап, `pf3ChartBox` в карточке | `chart.js` подключается после `app-5.js`; библиотека 5.0.8 с jsdelivr вместо unpkg 4.2.0; тема из CSS-токенов |
| `BOOK[]` — позиция: side, qty, entry, stop0, stop, target, opened | строки портфеля с `r[6]` qty, `r[9]` средняя; стопа/цели/стороны нет | `POS_META[tab][tk] = {side, stop0, stop, target, riskKr, opened}` в снапшоте; qty/средняя остаются в `r[]` (налог `pfTaxLots` не трогаем) |
| «Взвести лимит» / «Добавить в план» | `PLAN_RULES` {tk, act, level, ccy, qty, amount, deadline, note, tab, done, hitAt} | расширить полями `side, stop, target, riskKr, status`; `planStatus` проверяет `level` как раньше, плюс стоп/цель по позиции |
| Журнал: бэктест плана + «Мои сделки» | `PF_TRADES` {tab, tk, act, qty, price, plNative, date}; `bt*` (app.js:3479) | `SIG.backtestPlans` заменяет `bt*`; для сделок — связка вход→выход по `POS_META.opened` и R от `stop0` |
| `FX` в прототипе (пример 9.40) | `FX` глобал (app.js:295), `refreshFX` | без изменений |
| Экран «Сегодня» | `HOME_KEY` → `homeHTML` (app-4.js:266) | под флагом `desk` `renderAll` при `curIdx===HOME_KEY` зовёт `deskToday()` |
| Экран «Скринер» | нет; частично `homeBestBoardHTML`, классические таблицы | новая виртуальная вкладка `SCREEN_KEY`; таблица по всей вселенной с инспектором |
| Экран «Акция» | `pf3DetailHTML` (app-4.js:454, 19 блоков) | под флагом — `deskStock(tk)` (6 блоков); AI-блоки прототипа не заменяют, а сворачивают в `<details>` |
| Экран «Позиции» | саб-вкладки Портфель / Статистика / Структура / Состояние | `deskBook()` — книга; Структура и Статистика остаются вторичными вкладками книги |
| Экран «Журнал» | саб-вкладки Сделки / Бэктест / Симуляция | `deskJournal()`; Симуляция удаляется (проверка «на бумаге» = план сделки) |
| Токены `desk.css` (`--page/--panel/--ink/--long/--short/--warn/--hold`) | `styles.css` `--bg/--bg2/--text/--green/--red/--yellow` + режим `ui2` | `desk.css` объявляет свои токены и алиасы для старых имён (`--bg: var(--page)` …), чтобы старые компоненты под флагом выглядели согласованно; `ui2` отключается при `desk` |
| Рельса навигации и клавиши `/ 1–5 j k` | `.tabs` сверху, 9 иконок шапки | под флагом шапка сворачивается в один пункт «⋯» (настройки, доступ, промпты, выход) |
| RU-строки прототипа | `RT(ru,en)` / `T(key)` | при переносе каждую строку обернуть в `RT`; EN-перевод — этап чистки |

## 3. Что остаётся без изменений

Supabase auth, RBAC (`can`/`canTab`, `supabase-rbac.sql`), формат `ledger_state` (rev, realtime), `PF_TRADES` и налоговый расчёт, семейные портфели и `pf3MyPort`, AI-эндпоинты и промпты воркера (доступ из «Акции» и «Сегодня», не отдельные экраны), cron AI-портфеля, тесты `tests/run.sh` и pre-commit хук с авто-`?v=`, деплой воркера вручную.

## 4. Структура файлов после слияния

```
index.html      data.js → signals.js → chart.js → app.js … app-5.js → desk.js  (+ desk.css за флагом)
signals.js      чистый слой сигналов (из прототипа, с тестами)
chart.js        chartModel (чистая) + renderStockChart (из прототипа, S5)
desk.js         экраны Сегодня / Скринер / Акция / Позиции / Журнал + адаптеры secFromRow, histBars, POS_META
desk.css        токены и компоненты новой оболочки
app*.js         худеют по мере удаления движков и экранов (этап 6)
```

Порядок скриптов важен: `signals.js` до `app.js` (старые функции могут делегировать); `chart.js` — тоже до `app.js` (S5: `boot()` в конце `app-5.js` уже может рисовать карточку, а `chart.js` берёт `RT`/`SIG` только в момент вызова); `desk.js` после `app-5.js` (использует `DATA`, `FX`, `PLAN_RULES`, `RT`). Хук pre-commit проставит `?v=` новым файлам сам.

## 5. Слои слияния и как проверяем каждый

| Слой | Что делаем | Как проверяем | Откат |
|---|---|---|---|
| 0. Фундамент | quick wins аудита: `defer`, пин `supabase-js`, rev-проверка в `pushState`, чанки в `refreshLivePrices`, `requireAdmin` на `targets/chart/ydebug`, `loadRow` по admin `user_id`, `run.sh` с кодом возврата | тесты зелёные; `?action=version`; два устройства: правка на A видна на B без потерь | git revert по коммиту |
| 1. Воркер | OHLCV в `?history=`, кэш Cache API 10 мин, дедуп `yAuth`, счётчик FMP | `curl ?history=MU` содержит `o,h,l,v`; лог подзапросов < 50 на S&P 500 | старый клиент терпит новый формат |
| 2. Данные | `secFromRow`, `POS_META`, расширение `PLAN_RULES`, ключи в `snapshotState`, миграция с флагом | тест `snapshotState keys` + round-trip `applyRemoteState`; ledger после сохранения читается старой версией | ключи аддитивные, старый код их игнорирует |
| 3. Сигналы в тени | `signals.js` подключён; в списке v3 колонка «Вердикт v2» рядом со старой; Home считает оба рейтинга | 1–2 недели сравнения на реальных данных; тест паритета `phase` ↔ `pf3Criterion`; расхождения разбираются в `plans/signals-calibration.md` | колонка скрыта флагом |
| 4. График | `chart.js` в карточке вместо `drawChart`; попап удалён | свечи/объём/зоны/линии/RSI на 5 бумагах разных валют; resize без утечек (`getEventListeners` в devtools) | `drawChart` остаётся до конца этапа |
| 5. Экраны за флагом | `desk.js` + `desk.css`; `dash_desk=1` включает рельсу и 5 экранов; старые вкладки доступны без флага | сценарии: «что купить сегодня», «где вход по X», «что с позициями» — каждый ≤ 2 клика; телефон | флаг выключить |
| 6. Переключение и чистка | флаг по умолчанию on → удаление старых экранов, движков (§4 плана внедрения), `ui2`, редактора раскладки, мёртвого кода и 88 классов; `styles.css` → `desk.css` | тесты; `grep` по удалённым именам пуст; размер `app*.js` −30 % | последний коммит перед чисткой помечен тегом |
| 7. Сервер | cron `bookcheck`: стоп/цель/лимит по `POS_META`/`PLAN_RULES` → Telegram с гистерезисом 0.3·ATR; документация, `CLAUDE.md`, справка | сработавший лимит приходит в Telegram при закрытой странице | отключить cron-задачу |

## 6. Где закрываются находки аудита

| Слой | Находки (id из отчёта) |
|---|---|
| 0 | perf-load#1–2, data-model-sync#1, solo#1, security-rbac#1–3, worker#5, tests-quality#1, solo#5, stale-info#2/5/9–13 |
| 1 | goal-gap#8, worker#1–3, perf-load#9, solo#4 |
| 2 | goal-gap#1–3, data-model-sync#4/5/9, stale-info#3, tests-quality#5/10 |
| 3 | reco-logic-overlap#1–13, ux-ia#3/6, goal-gap#4/7, tests-quality#2/9, solo#3 |
| 4 | goal-gap#6, css-design-system#12, solo#2 |
| 5 | ux-ia#1/2/7–9/12/15–17, css-design-system#1–6/8/10/11, perf-load#5 |
| 6 | ux-ia#10/13/14, css-design-system#7/9/13, data-model-sync#12, stale-info#6/14/15, worker#9/10 |
| 7 | goal-gap#5/11, worker#4/6, security-rbac#6/7 |
| позже (крупная модель данных) | data-model-sync#2/3, tests-quality#3/6/7 |

## 7. Сессии Implementation (по CLAUDE.md — каждая отдельная)

| Сессия | Слой | Вход | Выход | Оценка |
|---|---|---|---|---|
| S1 | 0 | этот план | quick wins в `main`, тесты зелёные | 1 день |
| S2 | 1 | S1, деплой воркера пользователем | `?history=` с OHLCV, кэш, `WORKER_BUILD` bump, `verify-worker` | 0.5–1 день |
| S3 | 2 | S2 | `POS_META`, `PLAN_RULES v2`, тесты синка — ✅ 2026-09-10, итоги в §7.1 | 1 день |
| S4 | 3 | S3 | `signals.js` + тесты + теневая колонка; отчёт расхождений через 1–2 недели — ✅ код 2026-09-10, наблюдение идёт (`plans/signals-calibration.md`), итоги в §7.2 | 1 день + наблюдение |
| S5 | 4 | S2 | `chart.js` в карточке — ✅ 2026-09-10, итоги в §7.3 | 1 день |
| S6 | 5 | S3–S5 | `desk.js`/`desk.css` за флагом, 5 экранов — ✅ 2026-09-10, итоги в §7.4 | 3–4 дня |
| S7a | 6 | S6 | чистка, не зависящая от недели использования: ui2, редакторы, миграции, шрифты, мёртвый CSS, справка — ✅ 2026-09-10, итоги в §7.5 | 0.5 дня |
| S7b | 6 | S7a + неделя использования desk + конец тени | флаг снят, старые экраны и движки удалены, `styles.css` → `desk.css` (объём — §7.5) | 1.5 дня |
| S8 | 7 | S3 | cron `bookcheck` → Telegram — ✅ код 2026-09-10 (нужны деплой воркера и SQL), итоги в §7.6 | 1 день |

S1, S2 и S5 не зависят от дизайна и могут идти в любом порядке; S4 и S6 — ядро.

### 7.1 Итоги S3 (2026-09-10)

Сделано (слой 2, всё аддитивно — старый клиент читает ledger как раньше):
- **Синк:** новые ключи `posMeta`, `desk`, `schemaV` в `snapshotState`/`applyRemoteState`. Снапшот от клиента до S3 (без `posMeta`/`desk`) не затирает локальные данные: они остаются (или берутся из локального бэкапа `dash_bak_*`) и пушатся обратно.
- **Позиция:** `POS_META[tab][TK]={side, stop0, stop, target, riskKr, opened, planId}`; `posMetaNorm/Get/Set/Del`, `posLevelsCheck`, `posCalc` (P&L шорта зеркально, `rNow` от `stop0`, открытый риск до текущего стопа), `bookPositions`, `bookRiskState` (капитал = акции + кэш, риск 1 % на сделку, лимит книги 6 %), `qtyByRisk`, `DESK={riskPct, riskCapPct, shortOk}` + `deskNorm`.
- **План v2:** `planRuleNorm` (поля `side, stop, target, riskKr, status, createdAt`; `done` — источник правды), `planStatus` со стопом/целью/стороной (`hit: level|deadline|stop|target|invalid`; стоп-лосс выхода; «сетап сломан» у входа за стопом; у открытой позиции стоп/цель из `POS_META`), `planRR`, `planMarkOpen`. В старой вкладке «План» — поля «Стоп»/«Цель», действия «Шорт»/«Откупить шорт», R/R, «по риску N шт», кнопка «📍 Открыта», бейджи ⛔/🎯/✖.
- **Бумага:** `secFromRow` (ключ = биржевой символ + валюта) и `deskUniverse` (все разрешённые RBAC v3-вкладки, дедуп, `held` по моим портфелям); гейт свежести `PX_LIVE` — цена live только после живой котировки этой сессии (≤ 30 мин), сид/снапшот не помечается live.
- **Налог шорта:** `pfTaxLots` для сделок `short:true` признаёт результат в дату откупа по средней выручке открытых шорт-продаж (genomsnitt); лонг не изменился.
- **Схема:** `SCHEMA_V=1`, `migrateSchema()`; одноразовые сиды (Anna, Sergei, Gold and Silver, Small Cap, HEM, MU в пустом портфеле) выполняются только при `schemaV < 1` — удалённое больше не воскресает.
- **Тесты:** полный список ключей снапшота + round-trip `applyRemoteState` по каждому ключу + снапшот старого клиента; группы posMeta, posCalc, qtyByRisk, bookRiskState, plan v2, tax lots short pair, secFromRow & universe, migrateSchema seeds. app 368 / worker 122, порог `MIN_CASES_app=340`.

Находки аудита: закрыты **goal-gap#2** (стоп/цель на уровне данных и плана; Telegram — S8), **goal-gap#3** (размер по риску), **tests-quality#5**, **tests-quality#10**; частично **goal-gap#1** (сторона в позиции/плане/налоге; открытие шорта из «💸 Сделка» и шорт-вердикты — S4/S6), **data-model-sync#5** и **stale-info#3** (сиды гейтятся `schemaV`; сама цепочка миграций по-прежнему идёт в `init()` — перенос в один `migrateState` и удаление старых шагов — S7). Отложены: **data-model-sync#4** (вынос `val/insider/aiReco/tgFull/tgMeta` и производных полей из личного снапшота — затрагивает не-админов и shared_analysis, отдельная сессия «крупная модель данных») и **data-model-sync#9** (id колонок вместо индексов, `count`-геттер, стабильный id вкладки — там же).

Переходный риск: пока на каком-то устройстве открыта версия до S3, её запись сбрасывает `schemaV` → при следующем применении сиды выполнятся ещё раз (как было до S3). Обновите сайт на всех устройствах.

### 7.2 Итоги S4 (2026-09-10)

Сделано (слой 3, теневой режим — старые движки и их UI не менялись):
- **`signals.js`** в корне (грузится до `app.js`, глобал `SIG`): порт прототипа v2 1:1 по вердиктам/планам (сверено на 22 бумагах прототипа — 0 расхождений) плюс: флаги `earnings` (отчёт ≤ 3 дн блокирует buy/short), `no-short` (ручной флаг `DESK.shortOk` не выставлен — предупреждение, не блокер, §10#7), `stale-target` (> 30 %); `barsFromHist` (новый `{t,o,h,l,c,v}` и старый `{t,c}`); `SIG.cmp` — единая сортировка (группа → R/R → балл); `snapshot` → null при < 60 свечах. Исправлено в прототипе: флаг `wide` не мог сработать (стоп максимум ровно 3·ATR, проверка была `>`); причина перегрева по таргету писалась как «+-7 % над SMA200».
- **Адаптер** (`app-5.js`): `sigRowPhase` (мост паритета с `pf3Criterion`), `sigOpts` (риск 1 % капитала портфеля, FX, апсайд к эффективному таргету, stale, дни до отчёта из `?calendar=`, `shortOk`), `sigSnapRow` с мемо `SIGNALS` по (время свечей + входы) — повторные рендеры не пересчитывают (solo#3 для нового слоя), `sigEnsure` (пул 4, общий с графиком кэш `_histCache` `sym:2y`, неудачи не повторяются 10 мин), журнал тени `dash_sig_shadow` (localStorage, 14 дней) и `sigShadowReport` (markdown: матрица старый × v2, фазы, пороги, стабильность, таблица расхождений).
- **UI:** доп. колонка списка v3 **«Вердикт v2»** (`PF3_XDEF` `sig2`, сортируемая; пилюля «глиф + слово», R/R и лимит, «≠» при расхождении, план и причины в подсказке; trim вне книги = «Перегрев»); кнопка «📋 Отчёт расхождений v2» в меню колонок; столбец **v2** в «🏆 общем рейтинге» Home (топ-12, скрыт ≤ 600px).
- **Тесты:** группы signals core (эталоны ATR/RSI Wilder, SMA, collapse, limitForRR, barsFromHist), phase parity (20 строк, все 9 фаз), tradePlan (лонг/шорт, клэмпы, wide, half, qty = `qtyByRisk`), snapshot MU/AZN/AAPL на реальных свечах (`tests/fixtures-signals.js`, 260 баров), signals shadow adapter, pf3SignalInfo. app 447 / worker 122, `MIN_CASES_app=420`; `signals.js` добавлен в раннер, pre-commit хук и `check-syntax`.
- **Проверено вживую** (headless Chrome, боевой воркер): Nasdaq 100 + OMXS30 + портфель — 115 снимков без ошибок; день 0 отчёта: совпадение с «Рекомендацией» 60 %, фазы 100 %; вопросы калибровки Q1–Q10 — `plans/signals-calibration.md`.

Находки аудита: закрыты **goal-gap#7** (R/R в списке, ATR по High/Low — в колонке v2), **tests-quality#2** частично (`pf3Criterion`, `pf3SignalInfo` покрыты; `pf3Reco`, `bt*`, `aipVerdict` — уйдут в S7 вместе с движками), **tests-quality#9** для нового слоя (все пороги в `SIG.CFG`), **solo#3** для v2 (мемо снимка). **reco-logic-overlap#1–13**, **ux-ia#3/6** — единый слой есть, но старые движки удаляются только после калибровки (S7); **goal-gap#4** (скринер) — S6; **reco-logic-overlap#12** — v2 не зависит от случайно открытых карточек (свечи и календарь грузятся явно колонкой), старый `pf3Reco` по-прежнему зависит.

Дальше: 1–2 недели наблюдения → решения Q1–Q10 в `plans/signals-calibration.md` → сессия калибровки (правки `SIG.CFG`/правил + тесты); параллельно можно S5 (`chart.js`, маркеры — после Q4) или S8.

**Калибровка (2026-09-10, `SIG.VER = 2026-09-10-c1`):** пользователь принял рекомендации §3.1 на дне 0 (решения — `plans/signals-calibration.md` §5, итоги и прогон до/после — §6). Цель-фолбэк 2·ATR (`targetAtr`, флаг `atr-target`, «≈»), `maxStopAtr` удалён, `wide` > 2·ATR (подсказка), допуск 1e-9 на границе R/R, нож по вчерашнему S60, перегрев по таргету только над SMA50, undr над SMA200 → hold, пробой ≠ откат, stale-target > 50 % и только вниз; `SIG.sidePlan`/`rrOk`/`isBreakout` — чистые и покрыты; журнал тени пишет версию правил. На 114 бумагах: buy 6 → 14, short 0 → 1, сменили вердикт 10. `whatif-signals.js` без строковых патчей (`--ref=<git ref>` для базы). app 472 / worker 122. Дальше — S5 (маркеры = реплей `snapshot`, Q4) или S8.

### 7.3 Итоги S5 (2026-09-10)

Сделано (слой 4):
- **`chart.js`** (новый, грузится после `signals.js`): `chartModel` — чистая модель (окно свечей, SMA/RSI, зоны ±0.3·ATR вокруг 2+2 структурных уровней, линии Вход/Лимит/Стоп/«≈ Цель» по стороне или план открытой позиции «Средняя/Стоп/Цель» из `POS_META`, маркеры, итоги реплея окна); `renderStockChart` на lightweight-charts **5.0.8** с jsdelivr + SRI (`loadLWC`): свечи → объём (панель) → SMA 50/100/200 → зоны → линии → маркеры (сделки реплея v2, инсайдеры P/S, мои сделки из `PF_TRADES`) → RSI (панель) → легенда и тултип по crosshair (с событиями бара); тема из CSS-токенов, перерисовка при смене темы (`stockChartsRetheme` из `applyTheme`); `autoSize` — без слушателей `window`, `destroy()` при каждой перерисовке/закрытии.
- **Q4 (маркеры = реплей вердикта):** `signals.js` — `evalAt(bars, ind, i)` (вердикт на любом баре, причинно; `snapshot` = `evalAt` на последнем баре + `ind`), `simTrade` (вход по закрытию; стоп, при гэпе — по открытию; ½ на цели; +1R → безубыток; +2R → chandelier 2·ATR; в баре сначала стоп), `replay` (вход — бар, где вердикт впервые стал buy/short; позиции не перекрываются), `replayStats`. Старые `markers()`/`backtestPlans()` (SMA-пересечения прототипа) удалены; `snapshot` больше не считает маркеры (колонка v2 на 115 бумагах их не тратит). Реплей ~20–50 мс на 2 года, мемо на записи `_histCache`.
- **Связка (`app.js`):** `drawChart` → `stockChartDraw(state, boxId)` — один код для карточки v3 и попапа классических таблиц; `histBars` (кэш 10 мин общий с колонкой v2); переключатели Лонг/Шорт (`pf3SetSide`/`setChartSide`, без перерисовки) и 1Г/3Г (2y/5y); защита от гонки перерисовки (`_tok`); на карточке без позиции сторона — сторона вердикта. `pfPerfDraw` («Развитие портфеля») переведён на API v5 (`addSeries(LineSeries)`). Логотип TradingView оставлен — атрибуция требуется лицензией lightweight-charts.
- **Итоги реплея под графиком:** «Сигналы v2 на истории окна: N сделок · X % в плюсе · средний R · PF». По 116 бумагам (2y): 1253 сделки, 27 % в плюсе, в среднем +0.03R, PF 1.05 (лонг 1.08, шорт 0.92; с расчётной целью `atr-target` 1.01, со структурной — 1.12) — лучше SMA-правил прототипа (PF 0.91), но по сути в ноль; разбор — `plans/signals-calibration.md` §7.
- **Тесты:** `evalAt` ≡ `snapshot` на срезе (причинность), лестница `simTrade` (стоп, гэп, безубыток, ½ + трейлинг, открытая, шорт, стоп раньше цели в одном баре), `replay` (вход = смена вердикта, без перекрытий, вход по закрытию), `replayStats`, `chartModel` (окно, зоны, линии по стороне/позиции, «≈ Цель», маркеры в окне и сортировка, подписи только у последней сделки, инсайдеры/мои сделки, старый формат без объёма), `chartBarAt`, текст итогов. app 503 / worker 122, `MIN_CASES_app=480`; `chart.js` — в раннере, хуке и `check-syntax`.
- **Проверено вживую** (headless Chrome, локальный сервер + боевой воркер): карточки AMZN/AZN/NVDA/MU, тултип, Шорт без перерисовки, 3Г, светлая тема, попап VOLV B, телефон 390 px, закрытие карточки/попапа освобождает график, план позиции MU (кнопки стороны скрыты), маркеры инсайдеров и моих сделок, `pfPerfDraw` на синтетической истории; ошибок в консоли нет.

Находки аудита: закрыты **goal-gap#6** (свечи, объём, RSI, точки входа/выхода; интрадей — нет), **css-design-system#12** (высота из CSS, линии Вход/Стоп/Цель, маркеры без эмодзи), **solo#2** (утечка `resize` ушла вместе с `drawChart`); частично **reco-logic-overlap#8** (график рисует входы/выходы v2), **tests-quality#11** (LWC с пином и SRI), **css-design-system#2** (цвета графика из токенов, кроме палитры SMA §7).

Не сделано в S5 (по плану — S6/S7): попап классических таблиц остаётся (рисует тот же `chart.js`; удаляется вместе с таблицами), экран «Акция» и «История сигналов» в карточке, слои-переключатели графика в UI. Дальше — S6 (экраны за флагом) или S8 (bookcheck).

### 7.4 Итоги S6 (2026-09-10)

Сделано (слой 5, всё за флагом — без флага приложение прежнее, кроме кнопки 🖥 в шапке):
- **Флаг и оболочка.** `localStorage.dash_desk='1'`; `?desk=1`/`?desk=0` включает/выключает и запоминается (`deskFlagFrom`). Инлайн-скрипт `<head>` ставит `html.desk` до отрисовки (ui2 при этом не включается). `desk.js` грузится последним; при активном desk `renderAll`/`renderPF3` перерисовывают его (`deskRender`, дебаунс; фоновая перерисовка ждёт, пока фокус в поле ввода), старые экраны не рисуются, их поллеры остановлены. Классика — «⋯ → Классический вид» или кнопки «🗂 Карточка»/«Структура»/«Налоги»… (`deskClassic(tab, tk, sub)`) с плавающей «↩ Trade Desk»; «⋯ → Выключить» снимает флаг. Маршрут в хэше `#desk/<экран>[/<ключ бумаги>]` (назад работает).
- **Данные.** Вселенная — `deskUniverse` (все разрешённые RBAC вкладки: 430 бумаг из 13 вкладок на данных сида) + `src` строки-источника; снимки — `sigSnapRow` (мемо) с риском 1 % капитала выбранного портфеля. Свечи грузятся фоном пулом `sigEnsure` (книга → план → мои портфели → остальное; ~20 с на 430 бумаг, прогресс в шапке), догружаются при смене состава вселенной (вход, синк). Календарь отчётов — один запрос только для книги и сетапов на вход. Котировки моих портфелей + `planCheck` — при входе, по ⟳ и каждые 5 мин.
- **Экраны.** Сегодня: KPI (сетапы · ждать уровня · позиции требуют действия · отсеяно с причинами), «Вход сегодня» строками с глаголом первым (на телефоне — карточки), «Ждать уровня» с «Взвести», «Сократить в книге», «Позиции — внимание», взведённые планы, AI Proto. Скринер: вердикт/сторона/фаза/вкладка/у уровня/R/R ≥ 2/в книге/текст, сортировка (по умолчанию `SIG.cmp`), инспектор с мини-графиком и планом, j/k/Enter. Акция: график-герой (`stockChartDraw`, при перерисовке канвас переносится, Лонг/Шорт без перерисовки), план с полем «риск, kr» → размер, флаг «шорт доступен» (`DESK.shortOk`), сигнал и причины, лестница уровней, таргет свежий/средний, betyg, отчёт, инсайдеры 90 дн, новости, AI-разбор свёрнут (`aiRecoHTML`), история сигналов (реплей v2). Позиции: нетто/брутто, P&L, открытый риск против лимита книги, кэш и капитал, таблица с полосой «стоп ◆ цена → цель», R сейчас и действием лестницы выхода (`deskPosAct`: нет стопа · стоп · цель · перегрев · отчёт при < 1R · трейлинг при +2R · б/у при +1R · у стопа), кнопки «Принять стоп/цель из плана», «Стоп в б/у», «Трейл 2·ATR», «Стоп/цель…», «Продать/Откупить…»; фильтр по портфелю (одна книга, §10#8). Журнал: мои сделки «туда-обратно» из `PF_TRADES` (доля в плюсе, средний %, PF, итог), планы (`PLAN_RULES` со статусом, ✓/🗑), бэктест правил (реплей v2 по всей вселенной: 4743 сделки, 28 % в плюсе, +0.03R, PF 1.06 — лонг 1.16, шорт 0.76).
- **Исполнение.** «В план»/«Взвести лимит» пишет правило v2 в выбранный портфель (дубль той же стороны обновляется). «Исполнить…» — окно: портфель, кол-во, цена, дата, стоп, цель; лимит открытого риска книги блокирует кнопку (`deskCapCheck`, §4). `deskExecApply`: позиция (кол-во/средняя — genomsnittsmetoden, `pfApplyTradeSide`), комиссия, кэш (лонг — сумма ± комиссия; шорт — при открытии только комиссия, результат при откупе), `PF_TRADES` (шорт — `short:true`), `POS_META` (stop0 = первый стоп), план → `open`, закрытие в ноль удаляет мету и закрывает план. Новая строка берёт цену исполнения и копирует SMA/уровни/таргеты строки-источника по именам колонок (фаза и вердикт после покупки не меняются). Шорт открывается из desk — **goal-gap#1 закрыт** (сторона в позиции/плане/налоге/исполнении).
- **Приложение.** `pfEquitySEK` считает шорт результатом, а не стоимостью; `recalcPF` — P&L шорта зеркальный (стоимость `r[13]` по модулю, как раньше: классические итоги портфеля шорт пока показывают как лонг — уходят в S7); `stockChartsRetheme` перерисовывает графики desk.
- **Дизайн.** `desk.css`: токены §7 (light/dark), алиасы старых имён (`--bg/--text/--green…` → новые, поэтому тема графика и AI-блок согласованы), классы `dk-*`, один брейкпоинт 900 px (рельса → нижняя панель), Golos Text + JetBrains Mono (кириллица), `:focus-visible`, `color-scheme`. RU/EN через `RT`.
- **Тесты.** Группы desk core, pfApplyTradeSide, deskPosAct & trail, desk today & screener, desk journal, desk exec & equity (лонг/шорт, лимит книги, сторона, копирование колонок, налог шорт-пары), desk universe src. app 583 / worker 122, `MIN_CASES_app=560`; `desk.js` — в раннере (вырезается `deskBoot()`), хуке и `check-syntax`.
- **Проверено вживую** (headless Chrome, локальный сервер + боевой воркер, без входа — синк не писал): все 5 экранов в тёмной и светлой теме, 1440 px и 390 px (без горизонтальной прокрутки), инспектор и j, график AMZN (15 канвасов, Шорт без перерисовки), риск-поле, «В план» (шорт-лимит), исполнение лонга AMZN и шорта NCC B, правка стопа (stop0 сохранён), книга/журнал/планы/бэктест, меню, EN, классика → назад, выключение/включение флага, роль user без портфелей (только Nasdaq 100, без кнопок исполнения); ошибок в консоли нет.

Находки аудита: закрыты под флагом (окончательно — после удаления старых экранов в S7) **ux-ia#1/2/7/8/9/12**, **css-design-system#5/6/8/11**, **goal-gap#1**, **goal-gap#4** (скринер по всей вселенной); частично **ux-ia#15** (одна ⟳ в desk; старые кнопки — до S7), **ux-ia#16** (сервисные пункты — в «⋯»; вкладки классики — до S7), **ux-ia#17** (AI — блок «Акции» и кнопка «Сегодня»; AI-вкладки удаляются в S7), **css-design-system#1/2/3/4/10** (в desk — два слоя токенов, семантика рынка, контраст подписей, шрифты с кириллицей, фокус; ui2, DM Sans/Space Grotesk и старые правила — S7), **perf-load#5** (Golos Text добавлен, DM Sans/Space Grotesk ещё грузятся для классики — S7).

Не сделано в S6 (по плану — S7/S8): удаление старых экранов/движков/ui2/редактора раскладки и флага; EN-добивка старых строк; Telegram по стопам/лимитам без открытой страницы (S8 bookcheck); R по реальным сделкам (нужен стоп входа у старых позиций — появляется у новых); слои-переключатели графика. Дальше: неделя использования desk → S7 (чистка) или S8.

### 7.5 Итоги S7a (2026-09-10)

S7 разделена по решению пользователя: неделя использования desk (вход S7) не прошла — S6 сделан в тот же день, — а удаление старых движков прекращает теневое сравнение «Рекомендация ↔ v2». В S7a — только то, что не зависит от недели и тени; флаг, старые экраны и движки не тронуты.

Сделано (слой 6, часть 1):
- **ui2 — единственный вид классики.** Тумблер ✨, `toggleUI2/applyUI2/initUI2` и `localStorage.dash_ui2` удалены; инлайн-скрипт `<head>` ставит `html.ui2` всегда вне desk (desk.js при выходе и в `deskClassic` — тоже). Правила `html.ui2 …` не распрямлялись: базовые правила по-прежнему нужны старым компонентам внутри desk (AI-блок, классика через `deskClassic`) — схлопывание слоёв тем (css-design-system#1) — S7b вместе с переходом на `desk.css`.
- **Редакторы.** Удалены редактор раскладки ✏️ (`LAYOUT`, `eapply/erow/eord/eset`, `editMode`, drag-обвязка, кнопка и полоса, CSS `.edit-*`), редактор групп 🗂 (`toggleGroupsEditor/renderGroupsEditor/grpAdd/grpRename/grpDel/grpAssign`, оверлей) и перетаскивание вкладок (`tabDrag*/tabDrop*/reorderTab`). Порядок саб-вкладок, карточек сводки, блоков Home и карточек AI-Dashboard — фиксированный из кода. Группы классической навигации (`TAB_GROUPS`/`TAB_ORDER`) **только читаются** и синкаются, чтобы свои вкладки не потеряли группу; ключи уйдут в S7b вместе с навигацией. «🗑 Удалить вкладку» (жила только в редакторе групп) перенесена в меню «⚙ Колонки» своей вкладки. Перетаскивание колонок классической таблицы и списка покупок desk оставлено (таблица уходит в S7b; список покупок — фича I1).
- **Миграции → `migrateState()`** (data-model-sync#5, stale-info#3): один проход на загрузку — в `boot()` (бандл) и `applyRemoteState()` (облако), не в `init()` (его зовут ~16 мест: переименование, язык, вход…). В нём: `migratePortfolio3` (только сид MU при schemaV 0), `migrateNasdaqV3` (бандл → v3), `simMigrateTabs`, `migrateAiPort`, `restoreXcols`, `migrateSchema`. `fixCompanyNames` (производные имя/сектор/тип) остался в `init()`. Удалены шаги только для старых облачных состояний и **личные сиды**, которые попадали в облако нового аккаунта при первом входе: Портфель 2.0 (колонка, импорт, удаление, `d.removed`), снимок брокера 2026-06-10 (MU 5 @ 509.48, кэш 113 848, плечо 50 000), перенос AI-отчётов, Anna/Sergei с позициями и кэшем, заголовок «Portfolio (Dima)», Gold and Silver, Small Cap, HEM. Флаги этих шагов в данных (`brokerSnap`, `cashSnap`, `gsSeed`, `scSeed`, `aiMig`, `ttlMig`) **оставлены**: клиент до S7a без флага применил бы шаг заново (кэш, удалённые тикеры). `SCHEMA_V` не менялся (1).
- **Снапшот** (data-model-sync#12, частично): ключи `layout` и `aiPrefs` (и `AI_PREFS`) удалены — старый клиент без них ничего не теряет. `rankings`/`sma` — до удаления классических таблиц (S7b).
- **Шрифты** (css-design-system#4, perf-load#5): DM Sans и Space Grotesk (без кириллицы) → Golos Text во всём `styles.css`; из Google Fonts убраны 2 семейства (8 начертаний), Golos получил вес 800 (был faux-bold), добавлены preconnect к `fonts.googleapis.com`/`fonts.gstatic.com`.
- **Мёртвый CSS** (solo#5): 64 неиспользуемых класса → 84 правила (калькулятор ребалансировки `rb-*`, старые карточки `pf-*`, Home-чипы и фьючерс-плитки прошлой вёрстки, `tgt-bar*`, `pf3-cal-*` старого календаря и др.) + CSS редакторов и перетаскивания вкладок; проверка — скрипт по всем `app*.js`/`desk.js`/`chart.js`/`signals.js`/`index.html` с учётом динамических префиксов (`v-${…}`, `'rv-'+…`), после чистки кандидатов 0. Неопределённые токены (css-design-system#13): `--card`/`--card2`/`--surface2` → `--bg2`/`--bg3` (был прозрачный фон у маркеров и карточек), неиспользуемый `--muted` удалён. Пустой контейнер саб-вкладок на Home больше не рисует «пилюлю» (`#subTabs:empty`).
- **Справка и онбординг** (stale-info#6, ux-ia#14): раздел «Вкладки и виды» — Trade Desk вместо ✨, реальные блоки Home, «Структура» вместо «Сектора · Тип», Симуляция как вкладка, План/Сделки/Налоги, AI у каждого портфеля (без «🧠 памяти» — правила отменены); онбординг — меню слева, строка про Trade Desk; подзаголовок шапки без «Ребалансировки». Три комментария, противоречащих коду (stale-info#15), исправлены; карта файлов по доменам в `CLAUDE.md` (stale-info#14).
- **Тесты:** группа `migrateState` (schemaV 1 — ничего не засевается; schemaV 0 — только PF3 с MU, без чужих портфелей и заголовка; идемпотентность; бандл-индекс → v3; `init()` без миграций); round-trip глушит `migrateState` так же, как `init`; `SNAP_KEYS` без `layout`/`aiPrefs`; группа `eapply layout` удалена. app 734 / worker 152.
- **Размер:** `app.js` −26.8 КБ, `styles.css` −9.4 КБ, `index.html` −0.7 КБ; всего −645/+156 строк.
- **Проверено вживую** (headless Chrome, локальный сервер + боевой воркер, без входа — синк не писал): `html.ui2` без кнопок ✨/✏️/🗂, Golos Text загружен, 7 групп навигации без перетаскивания; бандл после `migrateState` — PF3 = [MU] без заголовка, без Anna/Gold, 8 индексов v3, schemaV 1; Home, сводка PF3 (7 карточек), меню «⚙ Колонки» своей вкладки с «🗑 Удалить вкладку» и само удаление, справка, онбординг, AI-Dashboard; desk вкл → «Классический вид» (ui2 есть) → назад → выкл (ui2 есть); телефон 390 px без горизонтальной прокрутки; ошибок в консоли нет.

**Объём S7b** (после недели использования desk и решения закончить тень): флаг `dash_desk` по умолчанию on → снят; удалить старые экраны (классические таблицы `renderTable/renderRanking` + колонки DnD, Home, AI-разборы/AI-Dashboard как вкладки, Симуляция, попап графика), движки `pf3Reco/pf3RecoHorizons/scenario*/homeBest*/homeForecastPicks/pf3Fcast12/atrFromCloses/bt*` (§4 плана внедрения; `aipVerdict` воркера — отдельно, нужен деплой) и теневой журнал; навигацию групп и ключи `tabGroups/tabOrder/rankings/sma`; ui2 и старые слои тем → `desk.css` (css-design-system#1/2/3/7/9); одна кнопка «Обновить» (ux-ia#15); служебные вкладки в «⋯» (ux-ia#16); EN старых строк; справка под одну IA; `grep pf3Reco\|scenarioShort\|homeBestComposite` пуст (§9). Перед чисткой — git-тег последнего коммита.

Не входит в S7 (отдельно): общий базовый промпт воркера и дедуп клиент↔воркер (worker#9/10 — нужен деплой, можно вместе с S8), inline-обработчики и `prompt/confirm` (ux-ia#13, L).

### 7.6 Итоги S8 (2026-09-10)

Сделано (слой 7; воркер `WORKER_BUILD = 2026-09-10s8-bookcheck`, **нужны деплой и SQL** — см. ниже):
- **bookcheck** (`telegram-notify.js`): cron читает ledger владельца и шлёт в Telegram одно сообщение за тик «📨 Книга: стопы и лимиты». Условия (`bookItems`, чистая): у позиций (qty > 0) — стоп и цель из `POS_META` или из open-правила плана; у правил плана (не `done`, не `open`) — лимит `level` (`sell` — цена ≥, иначе ≤) и стоп: у правила входа это «✖ сетап сломан» (как `planStatus` → `invalid`), у правила выхода — стоп-лосс. Строки: пробит стоп (R от `stop0`, P&L в kr), цель, лимит (размер, стоп/цель, R/R), заметка правила. AI-портфель пропускается (у него свои уведомления). Дедлайны плана в Telegram не идут — только цены.
- **Гистерезис 0.3·ATR** (`bookEval`, чистая): условие шлётся один раз; взводится заново, только когда цена отойдёт от уровня на 0.3·ATR(14) (без ATR — 1 % уровня). Ключ условия содержит уровень — перенос стопа/лимита взводит сразу; ключи удалённых условий отбрасываются. ATR — `atrLast` по тем же дневным свечам `yahooLite` (0 новых подзапросов; поле `atr` добавлено в ответ `yahooLite`/`?symbols=&lite=1`).
- **Состояние дедупа — `ai_state.book`** (клиент эту таблицу не видит и не затрёт); без колонки bookcheck не шлёт ничего (иначе повторял бы каждые 20 мин) и пишет причину в лог/`?action=version`. Порядок: сохранить состояние → Telegram; если Telegram не принял — отметки снимаются, следующий тик повторит.
- **Cron:** `pickCronTask` — :00 цикл AI-портфеля, :20 анализ PF3, :40 анализ Anna, :10/:30/:50 и минуты x5–x9 — bookcheck (при `*/10` раз в 20 мин, при `*/5` — каждые 5–10 мин). Проверяются только бумаги открытых сейчас рынков (`marketOpen`), до 20 символов (сначала стопы, потом цели, потом лимиты); подзапросов ≤ 25. Гейт анализа портфелей получил допуск −90 с (слот теперь один тик в час — иначе тик на секунды раньше сдвигал анализ на 2 ч).
- **Выключатель:** `DESK.tg` (`deskNorm`, по умолчанию вкл, выкл только явным `false`) — Trade Desk → ⋯ → «📨 Telegram-алерты». Воркер читает `snap.desk.tg`. Заодно `deskRiskEdit` больше не теряет `whatIf`/`tg` при смене риска.
- **Админ-роут** `?action=bookcheck` (Bearer): `&dry=1` — что сработало бы, без записи и Telegram; `&all=1` — без часов бирж. В `?action=version` — строка последнего прогона в изоляте.
- **Аудит:** закрыты **goal-gap#5** (алерты без открытой страницы — по стопам/целям/лимитам; сценарные алерты watchlist намеренно не возвращались — решение 2026-06-24 «состояние акций смотрим на сайте»), **worker#6** (`writeChecked` — обобщённый RMW с повтором; анализ портфеля шлёт Telegram только после коммита, `updateTargets` и обнуление AI-портфеля проверяют коммит), частично **worker#4** (cron пишет итог и ошибки в Workers Logs вместо `.catch(() => {})`; ретраи aiport/pfanalyze через `anthropicRun`, `stop_reason`, AbortController — не сделаны), частично **security-rbac#6** (новости: только http(s)-ссылки в воркере и на клиенте, заголовок/издатель экранируются; e-mail пользователя в «Доступе»; глобальные `escHtml`/`safeUrl` в app.js — остальные sink-и и 5 локальных `esc` не переведены). **security-rbac#7**: пин и SRI supabase-js были с S1, CSP не вводили (мешают inline-обработчики, ux-ia#13). **goal-gap#11** (гэп пре/пост-рынка в алертах) — не сделано: bookcheck берёт только регулярную цену и молчит вне сессии.
- **Тесты:** worker — группы `atrLast`, `bookcheck` (условия из снапшота, выбор символов, срабатывание/дедуп/гистерезис/дребезг/перенос уровня, шорт, «сетап сломан» гасит лимит, тексты, экранирование, бюджет подзапросов), новые слоты `pickCronTask`, фильтр ссылок новостей; app — `deskNorm.tg`, `escHtml`, `safeUrl`. app 737 / worker 190. Сквозной прогон `bookCheckRun`/`scheduled` в node с подменённым fetch (Supabase/Yahoo/Telegram): пробой → 1 сообщение, повтор и полоса — тишина, взвод после 0.3·ATR, сбой Telegram → повтор, нет колонки → не шлёт, `tg:false` → не шлёт. Живой Telegram не проверялся (нужен деплой).

**Чтобы включить:** (1) Supabase → SQL Editor → выполнить `ai-state.sql` (идемпотентно; добавляет `ai_state.book`); (2) задеплоить `telegram-notify.js`, проверить `?action=version` (build `2026-09-10s8-bookcheck`, строка bookcheck); (3) по желанию крон `*/5 6-22 * * 1-5` вместо `*/10` — bookcheck чаще, AI-задачи по-прежнему раз в час; (4) проверка из консоли сайта: `fetch(PRICE_PROXY+'?action=bookcheck&dry=1&all=1',{headers:{Authorization:'Bearer '+await sbToken()}}).then(r=>r.json())`.

**Доводка после первого живого `dry` (2026-09-10, build `2026-09-10s8b-rule-ccy`, `SCHEMA_V=2`):** прогон показал 9 условий и два ложных «лимита» у Anna (TTE ≤ 85.41, CEVI ≤ 179) — это таргеты: `planParseLevel` при переносе совета AI брал самое большое число. Новый разбор берёт только цену входа (фрагменты с «вход/лимит/зона/поддержка/откат к/докупить/у/от/на»; числа после «к/таргет/цель/потенциал/до/стоп», индикаторы SMA50/RSI/P/E, проценты и kr пропускаются; без явной цены входа — единственное число, иначе 0 → уровень вручную). Миграция v2 (`planFixAiRule`) пересчитывает уровень у нетронутых правил из AI (уровень = старому разбору `planParseLevelV1`) и берёт валюту из строки бумаги. Второй сбой — `INVE-B` без цены: в строке «INVE B», у AI «INVE-B» → правило получило USD. Тикеры теперь сравниваются через `planTkKey` (клиент: импорт, `planCurPrice`) и `bkKey` (воркер: валюта из строк раньше валюты правила). app 760 / worker 191.

Не сделано в S8: общий базовый промпт и дедуп клиент↔воркер (worker#9/10), ретраи/`stop_reason`/таймауты AI-вызовов (остаток worker#4), остальной XSS-проход (security-rbac#6), гэп пре/пост (goal-gap#11). Следующее по редизайну — S7b после недели использования desk.

## 8. Риски слияния

- **Потеря данных при синке во время двух версий клиента** — закрывается в S1 (rev-проверка) до любых изменений формата; ключи только аддитивные.
- **Расхождение вердиктов старого и нового движков** — теневой режим S4; решение по порогам принимает пользователь по отчёту расхождений, не код.
- **Шорт ломает налог** — `POS_META.side` не меняет `r[]`, `pfTaxLots` получает парные сделки как сейчас; тест с шорт-парой обязателен в S3.
- **Лимит 50 подзапросов** — чанки в S1, `yahooLite` для скринера, кэш в S2; при вселенной ~394 бумаг скринер работает по кэшу воркера, а не по живым запросам.
- **Параллельный план `plans/research-workspace.md`** — решено 2026-09-10: основа — Trade Desk со строками решений; из него в план входят гейты свежести данных и дедуп по биржевому символу (S2/S3); файл архивируется после S6.
- **Решения по спорным вопросам** зафиксированы в `plans/redesign-trading.md` §10 (флаг `dash_desk`, удаление ui2/редактора/групп, RU+EN, R/R ≥ 2.0 с лимитом при 1.2–2.0, риск 1 % капитала и ≤ 6 % книги, ручной флаг шорта, одна книга с фильтром, AI как блок, коммит в конце каждой сессии, старт S1 → S2, вселенная ~394).

## 9. Критерии готовности слияния

- [ ] Флаг снят, старые экраны и движки удалены, `grep pf3Reco\|pf3RecoHorizons\|scenarioShort\|homeBestComposite` пуст.
- [ ] Любая бумага любой вкладки открывается как «Акция» с графиком, планом лонг/шорт и размером по риску.
- [ ] Скринер показывает «N бумаг из M вкладок» со всеми фильтрами; телефон = «Сегодня» одной прокруткой.
- [ ] Позиция хранит сторону/стоп/цель; сработавший стоп приходит в Telegram без открытой страницы (код S8 готов; отметить после деплоя воркера, SQL и первого живого сообщения).
- [ ] `tests/run.sh` зелёный с группами signals, tradePlan, posCalc, backtestPlans, snapshot round-trip; тест K4 с шорт-парой.
- [ ] Отчёт аудита: все находки слоёв 0–7 помечены «закрыто» или «отложено с причиной».
